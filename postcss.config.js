// Tailwind v4 is handled by @tailwindcss/vite — do NOT add it here.
// autoprefixer handles vendor-prefix injection for SCSS/CSS output.
export default {
    plugins: {
        autoprefixer: {},
    },
};
